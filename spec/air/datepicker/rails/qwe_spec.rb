RSpec.describe Air::Datepicker::Rails::Qwe do
  it "has a version number" do
    expect(Air::Datepicker::Rails::Qwe::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
